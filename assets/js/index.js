// Bootstrap JS — imports the full bundle (Popper is included automatically).
// To cherry-pick only the components you need, replace this import with
// individual ones, e.g.:
//   import { Dropdown, Modal, Tooltip } from "bootstrap";
import "bootstrap";
